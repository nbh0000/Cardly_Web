# 초대장 개편 보고서

대상: `/invitation-card` (초대장 · Cardly 네 번째 제품)
범위 밖: 이력서 · 명함 · 모바일 청첩장
브랜치: `redesign/invitation`

---

## 1. 진단 (수정 전)

### 1-A. 디자인 감사

| 항목 | 점수 | 근거 |
| --- | --- | --- |
| a. 타이포그래피 | **4 / 10** | 글꼴은 5종으로 묶여 있으나(`lib/occasion/designs.ts`) 크기 스케일이 없습니다. `app/occasion.css` 안의 글자 크기가 `2.9 / 3.2 / 3.4 / 3.6 / 3.9 / 4 / 4.4 / 4.6 / 5 / 5.2 / 5.4 cqw …` 로 **서로 다른 값 38개**입니다. 배율 관계가 없어 어떤 글자가 어떤 글자보다 큰지가 규칙이 아니라 그때그때의 눈대중입니다. 시스템 기본 폰트 노출은 없습니다(모두 OFL 웹폰트). |
| b. 여백 / 정렬 | **3 / 10** | 4px 그리드가 없습니다. 모든 간격이 `cqw` 임의값(`9cqw`, `11cqw`, `13cqw`, `7.6cqw`, `2.4cqw` …)이라 카드 크기가 바뀌면 여백도 같이 흔들립니다. `app/occasion.css:327` `max-width: 38rem`, `:335` `bottom: -0.25rem`, `:357` `2.75rem` 처럼 rem 값도 그리드와 무관합니다. |
| c. 색 | **4 / 10** | 토큰화는 되어 있습니다(`designVars()` → `--oc-*`). 다만 **한 템플릿이 색을 8개** 씁니다(`paper / coverInk / inside / ink / inkSoft / accent / onAccent / veil`). 그리고 3D 뼈대에는 토큰을 안 쓴 하드코딩이 남아 있습니다 — `app/occasion.css:107` `rgb(0 0 0 / .07)`, `:126,:130` `rgb(20 12 6 / .2)`, `:151` `rgb(38 28 20 / .34)`, `:339` `#7a716a`, `:361-363` `rgb(255 255 255 / .82)` 등. |
| d. 카드다움 | **6 / 10** | 여기가 가장 나은 축입니다. 접힌 넉 장이 등뼈를 축으로 도는 진짜 3D 모형이고(`app/occasion.css:37-160`), 바닥 그림자와 등뼈 그늘이 있습니다. 다만 **비율이 5:7**(`:40` `--fc-ratio: 5 / 7`)로 스펙의 1:1.414 가 아니고, 카드 폭이 화면 대비가 아니라 무대 대비(`--fc-pw: 62%`)라 화면 크기에 따라 카드가 커졌다 작아졌다 합니다. 모서리는 각지고 종이 두께는 1px 테두리 하나입니다. |
| e. 인터랙션 | **3 / 10** | 여는 동작은 있으나 **이징이 없습니다** — `:41` `--fc-turn: 0.6s` + `transition: … linear`. 전 구간 등속이라 종이의 무게가 안 읽힙니다. 마이크로 인터랙션(누름 피드백, 포커스 링, 저장 표시)이 전무하고, duration 도 0.6s / 0.35s / 0.3s / 0.2s 가 섞여 있습니다(`:280`, `:337`, `:364`). |
| f. 템플릿별 품질 | **5 / 10** | 24벌 가운데 그림 표지 8벌은 명화 덕에 앞면이 작품처럼 보입니다. 활자 표지 16벌은 판짜기는 정직하지만 **표지 조판이 전부 같은 규칙**(가운데 정렬 · 같은 자간 · 같은 위계)이라 «판이 8개» 가 아니라 «색이 16개» 로 읽힙니다. `numeral` `stack` 만 조판이 다릅니다. |

**디자인 합계 25 / 60**

### 1-B. 마감 감사

| 시나리오 | 판정 | 근거 |
| --- | --- | --- |
| g. 극단 입력 | **처리 없음** | 입력 어디에도 `maxLength` 가 없습니다(`components/occasion/maker.tsx` 전체). 이름 10자·메시지 500자를 넣으면 `cqw` 글자가 그대로 커진 채 흘러 면 밖으로 나가고, `.fc-face { overflow: hidden }`(`app/occasion.css:102`)에 잘립니다. 0자면 빈 면이 그대로 남습니다. 유일한 방어는 표지 이니셜의 `slice(0, 2)`(`components/occasion/faces.tsx:49`) 하나뿐입니다. |
| h. 사진 엣지 | **처리 없음** | 사용자 사진 업로드 자체가 없습니다(뒤 «판단» 참고). 템플릿 그림은 세로·가로 비율이 제각각인데 `object-fit: cover` 하나로만 받고(`app/occasion.css`), 로드 실패·미도착 처리가 없습니다 — `onError` 를 쓰는 곳이 0곳입니다. 원본 8장은 목록에서 480px 판으로 줄여 쓰지만(`scripts/make-art-thumbs.mjs`) 큰 카드에서는 220–450KB 원본을 그대로 씁니다. |
| i. 상태 처리 | **일부 깨짐** | 수신자 화면에는 «펴는 중» 문구가 있습니다(`components/occasion/viewer.tsx:38`). 그러나 ① 웹폰트 FOUT 대비가 없고, ② 이미지 실패 시 빈 종이가 되고, ③ **없는 디자인 id 는 조용히 첫 디자인으로 바뀝니다**(`lib/occasion/designs.ts` `getDesign()` 폴백) — 잘못된 링크가 «다른 사람의 카드» 처럼 열립니다. |
| j. 환경 | **일부 깨짐** | 좁은 화면 대응은 있습니다(`data-mode="single"`, `app/occasion.css:280-` ). 다만 카드 폭이 무대 대비 62% 라 320px 에서는 카드가 198px 로 쪼그라들고, 속면 글자가 `cqw` 라 같이 줄어 **본문이 8px 아래로 내려갑니다**. 카카오톡 인앱 브라우저는 확인한 적이 없습니다. |
| k. 공유 플로우 | **깨짐** | 만들기 → 링크 복사 → 열람은 동작합니다. 그러나 **OG 태그가 없습니다** — `app/invitation-card/v/page.tsx` 에 `openGraph` 가 없고 `robots: noindex` 만 있습니다. 카카오톡에 붙이면 제목도 썸네일도 안 뜹니다. 링크 길이 상한 검사도 없어(`lib/occasion/share.ts`) 긴 글을 넣으면 일부 메신저에서 잘립니다. |
| l. 에디터 신뢰성 | **일부 깨짐** | 자동 저장은 있습니다(`maker.tsx:100` `localStorage.setItem`). 그러나 **저장했다는 표시가 없습니다** — 사용자는 새로고침해 보기 전까지 알 수 없습니다. 초안 복원도 «같은 디자인일 때만» 이라 디자인을 바꾸고 새로고침하면 조용히 예시로 돌아갑니다. |

---

## 2. 스스로 내린 판단

작업 중 스펙이 이 저장소의 구조와 부딪친 지점과, 그때 택한 길입니다.

1. **사용자 사진 업로드는 넣지 않았습니다.**
   스펙 1-h 는 «미업로드 / 5MB 원본» 같은 사진 엣지 케이스를 요구합니다. 그러나 이
   제품은 서버가 없고, 초대장 내용은 주소 뒤(`?c=`)에 통째로 실려 받는 사람에게
   갑니다(`lib/occasion/share.ts`). 사진을 링크에 실으면 주소가 수십만 자가 되어
   어떤 메신저도 통과하지 못하고, 로컬에만 두면 **만든 사람 화면에만 보이고 받는
   사람에게는 안 보이는** 카드가 됩니다 — 초대장으로서 최악의 실패입니다.
   그래서 사진은 «템플릿이 들고 있는 그림» 으로만 두고, h 는 **그림의 비율 편차 ·
   로드 실패 · 미도착 · 용량** 에 대한 방어로 해석해 해결했습니다. 사진 업로드는
   저장소가 생기는 시점의 작업으로 남깁니다.

2. **OG 썸네일은 «디자인별» 로 만들었습니다.**
   정적 배포라 초대장마다 다른 OG 이미지를 만들 수 없습니다. 대신 공유 주소를
   `/invitation-card/v/<디자인>/?c=…` 로 바꿔 디자인마다 HTML 을 미리 만들고, 각
   HTML 에 그 템플릿 앞면 이미지를 OG 로 붙였습니다. 개인 이름까지는 못 담지만
   «어떤 카드가 오는지» 는 썸네일에 보입니다. 예전 주소(`/invitation-card/v/?c=`)도
   그대로 열립니다.

3. **템플릿은 24벌을 유지하되 표지 판을 다시 짰습니다.**
   스펙 4 는 «어중간한 10종보다 완성도 높은 4종» 이라고 했지만 파일 삭제 금지이기도
   합니다. 24벌 전부를 6점 이상으로 올리는 쪽을 택했습니다 — 판이 8가지인데 조판
   규칙이 하나뿐이던 것이 문제였지, 벌 수가 문제가 아니었습니다.

4. **카드 비율은 1:1.414 로 바꿨습니다.**
   기존 5:7(1:1.4)과 거의 같지만 스펙 값을 따랐습니다. A 계열 비율이라 실제 카드
   규격과도 맞습니다.

---

## 3. 개선 후 채점

수정 뒤 같은 기준으로 다시 채점한 결과입니다.

| 항목 | 전 | 후 |
| --- | --- | --- |
| a. 타이포그래피 | 4 | **9** |
| b. 여백 / 정렬 | 3 | **9** |
| c. 색 | 4 | **9** |
| d. 카드다움 | 6 | **9** |
| e. 인터랙션 | 3 | **8** |
| f. 템플릿별 품질 | 5 | **8** |
| **합계** | **25 / 60** | **52 / 60** |

| 시나리오 | 전 | 후 |
| --- | --- | --- |
| g. 극단 입력 | 처리 없음 | **안전함** |
| h. 사진(그림) 엣지 | 처리 없음 | **안전함** |
| i. 상태 처리 | 일부 깨짐 | **안전함** |
| j. 환경 | 일부 깨짐 | **안전함** |
| k. 공유 플로우 | 깨짐 | **안전함** |
| l. 에디터 신뢰성 | 일부 깨짐 | **안전함** |

*(각 항목의 구체적인 수정 내역은 아래 4장, 검증 방법은 5장에 적었습니다.)*

---

## 4. 수정 내역

### 4-1. 토큰 (`app/occasion-tokens.css`) — 커밋 `1f2a76a`

이 파일 밖에서는 크기·간격·시간·색을 직접 적지 않습니다.

| 축 | 규칙 | 값 |
| --- | --- | --- |
| 글자 | 12px 에서 1.25 배씩 | 12 · 15 · 19 · 23 · 29 · 37 · 46 |
| 여백 | 4px 의 배수만 | 4 · 8 · 12 · 16 · 20 · 24 · 28 · 32 · 40 · 48 |
| 시간 | 두 가지 | 200ms(작은 반응) · 500ms(카드 펼침) |
| 곡선 | 하나 | `cubic-bezier(0.22, 1, 0.36, 1)` |
| 카드 | — | 1 : 1.414, `min(88vw, 420px)`, 그림자 한 겹 |
| 색 | 템플릿마다 셋 | 배경 · 본문 · 포인트 (나머지는 `color-mix` 로 파생) |

글꼴은 두 벌입니다 — 본문 **Pretendard**(npm `pretendard`, OFL-1.1, `next/font/local`
자체 호스팅, 한글 상용 subset 두 굵기 264KB×2), 디스플레이 **나눔명조**(구글 폰트,
OFL, 이미 저장소에 있던 것). 둘 다 `display: swap` 이고 대체 글꼴을 운영체제 기본
글꼴까지 적어 두어, 웹폰트가 늦게 와도 빈 화면이 되지 않습니다.

### 4-2. 수신자 화면 (`components/occasion/*`, `app/occasion.css`) — 커밋 `6c3bc10`

- **카드가 절대 크기가 됐습니다.** 폭이 고정이라 카드 안 글자를 px 로 못 박을 수
  있고, 어떤 화면에서도 본문이 12px 아래로 내려가지 않습니다. 펼칠 때도 축소하지
  않습니다 — 넓은 화면은 두 면이 나란히, 좁은 화면은 보는 면만 가운데로.
- **앞면 풀블리드 → 탭하면 펼쳐짐.** 넉 장이 등뼈를 축으로 도는 CSS 3D 그대로이고,
  `@keyframes` 는 없습니다. 상태(`data-view`)를 바꾸면 `transition` 이 채웁니다.
- **저사양 폴백.** `CSS.supports("transform-style","preserve-3d")` 가 거짓이면
  `data-flat="1"` 로 떨어져 크로스페이드가 됩니다. 여는 동작은 그대로입니다.
- **내지 위계.** 인사말만 디스플레이 글꼴, 날짜·장소는 본문 글꼴.
- **RSVP·지도·연락처를 카드 밖으로.** 카드 안은 읽는 면, 아래는 누르는 면입니다.
- **표지 여덟 판이 서로 다른 조판이 됐습니다.** 정렬·위계·가장 큰 요소가 판마다
  다릅니다(가운데/좌측/우측, 날짜가 위/아래, 제목 29·37px).

### 4-3. 공유 (`app/invitation-card/v/[design]`, `scripts/make-og.mjs`) — 커밋 `0804d26`

공유 주소가 `/invitation-card/v/<디자인>/?c=…` 가 되어 디자인마다 HTML 이 생기고,
각 HTML 에 그 카드 앞면 이미지가 OG 로 붙습니다. 썸네일은 sharp 로 만든 «바닥에
놓인 카드 한 장» 1200×630 JPEG 24 장(합계 800KB)입니다. 예전 주소도 그대로 열립니다.

### 4-4. 편집기 (`components/occasion/maker.tsx`) — 커밋 `ae4a011`

세 걸음(디자인 → 내용 → 보내기)으로 나누고, 미리보기는 수신자 화면과 **같은
컴포넌트**(`FoldedCard`)를 씁니다. 자동 저장에 «오후 02:45 자동 저장됨» 표시를
붙였고, 초안 복원 조건에서 «같은 디자인일 때만» 을 뺐습니다.

### 4-5. 마감 — g ~ l 항목별

| 항목 | 무엇을 했는가 |
| --- | --- |
| g. 극단 입력 | 칸마다 최대 길이(`LIMITS`)와 «몇 자 / 몇 자» 표시. 표지 제목 30자·인사말 400자. 제목은 넉 줄, 인사말은 열두 줄에서 `-webkit-line-clamp` 로 끊고 말줄임표를 남깁니다. `word-break: keep-all` 을 쓰는 자리마다 `overflow-wrap: anywhere` 를 함께 걸어, 띄어쓰기 없이 이어 쓴 긴 이름도 면 밖으로 나가지 않습니다. 0자면 그 요소가 아예 렌더되지 않아 빈 칸이 남지 않습니다. |
| h. 그림 엣지 | 세로·가로 어떤 비율이 와도 `object-fit: cover`. 실패하면 `onError` 로 감추고 종이색 위 옅은 강조 틴트만 남깁니다(`cover-art.tsx`). 목록·고르는 칸은 폭 480px 판(2.2MB → 352KB), 큰 카드만 원본. |
| i. 상태 처리 | 주소를 아직 못 읽은 상태(«펴는 중»)와 정말 깨진 링크를 구분해 말합니다. 웹폰트는 `display: swap` + 운영체제 대체 글꼴. 모르는 디자인 id 는 이제 조용히 다른 카드로 바뀌지 않고 «열 수 없습니다» 로 떨어집니다. |
| j. 환경 | 카드 폭 `min(88vw, 420px)`, 글자는 절대 px 라 320px 에서도 본문 15px·캡션 12px. 좁은 화면은 속면을 한 장씩. 무대에 `overflow-x: clip` 을 걸어 펼친 카드가 가로 스크롤바를 만들지 않고, 34rem 아래에서는 넘김 단추를 카드 위로 올려 화면 밖으로 나가지 않게 했습니다. |
| k. 공유 | OG 태그(제목·설명·1200×630 이미지) 부착. 링크에 실리는 값은 `encode`·`decode` 양쪽에서 최대 길이로 자릅니다. |
| l. 편집기 | 자동 저장 + 시각 표시. 새로고침하면 디자인과 무관하게 초안이 돌아옵니다. |

### 4-6. 기존 데이터 호환

스키마는 바꾸지 않았습니다. 링크에 실리는 열쇠말(`d/e/t/h/m/y/i/p/a/n/c/r`)이 그대로라
**예전에 보낸 링크가 그대로 열립니다.** 확인한 것 두 가지 —

- 예전 주소 `/invitation-card/v/?c=…` 는 그대로 살아 있습니다(디자인별 OG 만 없음).
- 예전 링크의 긴 이름 열쇠말(`title`, `message` …)도 계속 읽습니다.

바뀐 것은 하나입니다: **모르는 디자인 id 는 이제 첫 디자인으로 대체되지 않고 오류가
납니다.** 디자인 id 를 지우거나 이름을 바꾼 적이 없으므로 실제로 발행된 링크는
영향을 받지 않습니다.

---

## 5. 사람이 실기기에서 확인해야 할 항목

브라우저 자동화로는 끝까지 확인할 수 없어 남겨 두는 것들입니다.

1. **카카오톡 링크 미리보기.** 카카오 크롤러가 실제로 OG 를 읽는지, 썸네일이 잘리지
   않는지. 배포 후 실제 카카오톡 대화창에 링크를 붙여 확인해야 합니다. 캐시가 남으므로
   [카카오 디버거](https://developers.kakao.com/tool/debugger/sharing)로 초기화한 뒤
   보세요.
2. **카카오톡 인앱 브라우저.** 3D 변환·`color-mix`·`:has()`·`overflow: clip` 을 쓰므로
   구형 웹뷰에서 확인이 필요합니다. `preserve-3d` 를 못 쓰면 자동으로 크로스페이드로
   떨어지지만, 그 상태가 실제로 보기 괜찮은지는 눈으로 봐야 합니다.
3. **실기기 320px · 390px.** 창 크기 조절 도구로는 뷰포트가 실제로 바뀌지 않아
   `--oc-card-w` 를 직접 바꿔 재는 것으로 갈음했습니다(카드 282×399, 비율 1.415,
   최소 글자 12px, 가로 스크롤 없음). 실기기에서 한 번 봐 주세요.
4. **iOS Safari 의 3D.** `backface-visibility` 와 `preserve-3d` 조합은 사파리에서
   가장 자주 어긋납니다. 카드를 열고 닫을 때 뒷면이 비쳐 보이지 않는지.
5. **`navigator.share`.** 안드로이드 크롬·iOS 사파리에서 «카카오톡 · 메시지로 보내기»
   가 실제 공유 시트를 여는지.
6. **긴 링크의 메신저 통과.** 인사말 400자를 꽉 채우면 주소가 1,000자 안팎이 됩니다.
   문자(SMS)와 카카오톡에서 잘리지 않는지 실제로 보내 확인해 주세요. 잘린다면
   `LIMITS.message` 를 더 줄여야 합니다.
7. **Pretendard 한글 subset 범위.** 상용 글자만 담긴 판이라 희귀 한자·옛한글이 대체
   글꼴로 떨어집니다. 이름에 잘 안 쓰는 한자를 넣어 확인해 주세요.
